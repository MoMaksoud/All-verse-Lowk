{
  "name": "marketplace-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-platform marketplace app with shared UI components",
  "scripts": {
    "build": "turbo run build",
    "postinstall": "pnpm --filter @marketplace/types build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "turbo": "^1.11.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "zod": "^4.1.5"
  }
}
